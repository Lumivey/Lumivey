# v0 approval writer checkpoint — 2026-09-18

Scope: draft PR #49 only. No production/main deployment, no paid v0 call, no synthetic customer approval written.

Implemented `lib/lumivey/v0-preview-approval.ts`: pure fail-closed approval writer contract requiring independent server session authentication, current owner approval privilege, a finalized immutable revision looked up from server storage, and an atomic exact insert. Client-submitted owner IDs, email, hashes, checkbox or localStorage cannot provide authority. Existing approval may be idempotently recognized only on exact match; a conflicting or revoked record must fail. Tests `tests/v0-preview-approval.test.mjs` cover unauthorized access, tenant mismatch, mutable/missing artifacts, forged fields, conflicts and backend errors. Safety workflow includes tests and its unit job succeeded for commit b034bc85a9fb14889ef0f59d91358d1b03a10f74.

**Live capability is still BLOCKED, not achieved.** Source inspection: `app/page.tsx` currently stores a supposed approved impression in browser localStorage; `app/prepare/page.tsx` stores a test email locally; this is neither identity verification nor a durable approval. `app/api/preview/route.ts` receives a browser-supplied understanding and generates an impression without trusted owner binding. Neon `get_auth` for the PR #49 Preview branch returned `Neon Auth is not enabled for this branch`. The repository's package.json does not contain a session/auth or Postgres driver. No trusted immutable final revision repository, authenticated approval route or DB adapter is integrated. Database approvals/revocations exist but have zero rows, zero RLS policies and no worker grants; the NOLOGIN limited role cannot connect. These limitations prohibit claims of a genuine approval or demonstrated A/B database isolation.

Next technical sequence: select and implement a verifiable server session provider (or a tightly scoped operator-only auth design), owner membership and immutable final-revision storage; implement the approval adapter transaction with exact insert and revocation guard; replace misleading browser-only `approved` wording before activating any submit operation. Then provision auditable limited LOGIN credentials and scoped RLS, perform separate-session A/B integration tests and two-connection idempotency tests, and only afterward restore ONE protected build route. Do not enable Neon Auth, issue credentials, change Vercel variables, loosen RLS or activate seven paid routes merely to make tests pass. No private secrets should be printed or committed.
